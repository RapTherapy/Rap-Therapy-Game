@page
@model RapGame.Pages.Frame185Model
@inject RapGame.Utils.MediaHelper mediaHelper
@{
    ViewData["Title"] = Model.MediaData.Title;
}

<div class="container">
    <div class="row justify-content-center pt-3 animate__animated animate__fadeInUp animate__slower animate__delay-1s">
        <div class="col display-3 text-center">
            <b>Well done!​ <br>
            You have just obtained three Mystic Mics!​</b>
        </div>
    </div>

    <div class="row justify-content-center pt-5  ">
        <div class="col-4 text-center">
            <img class="img w-30 animate__animated  animate__flip animate__slower" src="@mediaHelper.GetMediaPath(@Model.MediaData.PathsToImages[0])"/>
        </div>
          <div class="col-4 text-center">
            <img class="img w-30 animate__animated  animate__flip animate__slower" src="@mediaHelper.GetMediaPath(@Model.MediaData.PathsToImages[0])"/>
        </div>
          <div class="col-4 text-center">
            <img class="img w-30 animate__animated  animate__flip animate__slower" src="@mediaHelper.GetMediaPath(@Model.MediaData.PathsToImages[0])"/>
        </div>
    </div>
</div>

